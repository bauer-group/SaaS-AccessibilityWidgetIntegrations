{% sw_extends '@Storefront/storefront/layout/meta.html.twig' %}

{% block layout_head_javascript_tracking %}
  {{ parent() }}

  {% set pos = config('AccessibilityWidget.config.position') ?: 'bottom-right' %}
  {% set loc = config('AccessibilityWidget.config.locale') ?: 'auto' %}
  {% set col = config('AccessibilityWidget.config.primaryColor') ?: '#0058a3' %}

  <script>
    window.AccessibilityWidgetConfig = {
      corePath: '{{ asset('bundles/accessibilitywidget/accessibility-widget-core.min.js', 'asset') }}',
      cssPath:  '{{ asset('bundles/accessibilitywidget/accessibility-widget.min.css', 'asset') }}',
      position: {{ pos|json_encode|raw }},
      locale:   {{ loc|json_encode|raw }},
      primaryColor: {{ col|json_encode|raw }}
    };
  </script>
  <script src="{{ asset('bundles/accessibilitywidget/accessibility-widget-loader.min.js', 'asset') }}" defer data-aw-loader="1"></script>
{% endblock %}
